What happens when you initialize a repository? Why do you need to do it?
	
	It gets initialize, but it is a hidden folder. To initialize the Initial Commit, we use git init.
	
How is the staging area different from the working directory and the repository?
What value do you think it offers?

	Staging area is used for the specific files from the directory needs to be commit in the repository.
	Without the staging area, you cannot commit directly from the working directory

How can you use the staging area to make sure you have one commit per logical
change?
	
	It helps to make sure that the changes are all saved in the repoistory. That' why one commit per logical change

What are some situations when branches would be helpful in keeping your history
organized? How would branches help?

	Branches. It will help you keep updated and organized about the history, by self-checking out master branch.
	It also helps you save data if you are doing different type of programming in the same file. 
	It also helps you to provide the privacy of the data.

How do the diagrams help you visualize the branch structure?

	It helps to creat new braches and helps to understand the previous branches.

What is the result of merging two branches together? Why do we represent it in the diagram the way we do?

	Two branches are merged and combines the feature. It helps to understand the structure of the code and the changes made and changes need to be change.

What are the pros and cons of Git’s automatic merging vs. always doing merges manually?

	Git automatic merge helps you find the conflict of merging in the file, then correct it.
	In manual merge, things get complicated than the git merge.
	If it is a few lines of code in the file then it easier to merge manually, but if it is otherwise, things are complicated in manual merge.